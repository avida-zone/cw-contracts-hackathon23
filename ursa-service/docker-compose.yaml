version: "3.7"
services:
  postgres:
    image: postgres:14
    environment:
      - POSTGRES_USER=avida
      - POSTGRES_PASSWORD=avidapass123
      - POSTGRES_DB=postgres
    ports:
      - "5243:5432"
    volumes:
      - creds:/var/lib/postgresql/data

        # ursa-service:
        #   image: avida
        #   environment:
        #     - DATABASE_URL=postgresql://avida:avidapass123@localhost:5243/postgres
        #   ports:
        #     - "8000:8000"

volumes:
  creds:
