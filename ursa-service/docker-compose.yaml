version: "3.7"
services:
  postgres:
    image: postgres:14
    environment:
      - POSTGRES_USER=avida
      - POSTGRES_PASSWORD=avidapass123
      - POSTGRES_DB=postgres
    ports:
      - "5243:5432"

volumes:
  creds:
